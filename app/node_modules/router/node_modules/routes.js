'use strict';

module.exports = {
    '/': function (callback) {
        console.log('callback for \'/\'');
        callback(null, 'Index');
    },
    '/:id': function (id, callback) {
        console.log('callback for \'' + id + '\'');
        callback(null, 'Other', {'id': id});
    }
};