var React = require('react');

var Renderer = React.createClass({
    render: function () {
        return <p className="debug">
            Rendered on the <strong>{this.props.renderer}</strong>.
        </p>;
    }
});

module.exports = Renderer;